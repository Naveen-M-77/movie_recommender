# GitHub Actions CI for a React application
# This workflow installs dependencies, runs lint/tests, and builds the production bundle.
name: React CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    # Add legacy OpenSSL provider for webpack compatibility on Node >=17
    env:
      NODE_OPTIONS: '--openssl-legacy-provider'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          # Specify the Node.js version you use in your project
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        # Using npm install to avoid lockfile mismatch errors
        run: npm install

      - name: Run ESLint (if configured)
        run: |
          if [ -f .eslintrc.js ] || [ -f .eslintrc.json ]; then
            npm run lint;
          else
            echo 'No ESLint config found, skipping lint.';
          fi

      - name: Run tests
        # Allow exit with code 0 even if no tests are found
        run: |
          npm test -- --watchAll=false --ci --passWithNoTests

      - name: Build production bundle
        # Disable CI environment to prevent warnings treated as errors
        run: |
          CI=false npm run build

      # Optionally add deployment steps here
